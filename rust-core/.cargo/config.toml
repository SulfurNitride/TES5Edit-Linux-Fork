# =============================================================================
# Cargo configuration for xEdit Rust workspace
# =============================================================================
# Cross-compilation settings for building Windows targets from Linux.

[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
